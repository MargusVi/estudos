for(let i = 0; i <= 50; i++)
{
    if(i === 0)
    {
        console.log(i);
    }
    else if(i % 2 === 0)
    {
        console.log(`${i}, par`);
    }
    else
    {
        console.log(`${i}, ímpar`);
    }
}